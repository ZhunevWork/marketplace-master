/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { MarketplaceService } from "../marketplace.service";
import { MarketplaceCreateInput } from "./MarketplaceCreateInput";
import { Marketplace } from "./Marketplace";
import { MarketplaceFindManyArgs } from "./MarketplaceFindManyArgs";
import { MarketplaceWhereUniqueInput } from "./MarketplaceWhereUniqueInput";
import { MarketplaceUpdateInput } from "./MarketplaceUpdateInput";
import { MarketplaceSyncFindManyArgs } from "../../marketplaceSync/base/MarketplaceSyncFindManyArgs";
import { MarketplaceSync } from "../../marketplaceSync/base/MarketplaceSync";
import { MarketplaceSyncWhereUniqueInput } from "../../marketplaceSync/base/MarketplaceSyncWhereUniqueInput";

export class MarketplaceControllerBase {
  constructor(protected readonly service: MarketplaceService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Marketplace })
  async createMarketplace(
    @common.Body() data: MarketplaceCreateInput
  ): Promise<Marketplace> {
    return await this.service.createMarketplace({
      data: data,
      select: {
        apiKey: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Marketplace] })
  @ApiNestedQuery(MarketplaceFindManyArgs)
  async marketplaces(@common.Req() request: Request): Promise<Marketplace[]> {
    const args = plainToClass(MarketplaceFindManyArgs, request.query);
    return this.service.marketplaces({
      ...args,
      select: {
        apiKey: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Marketplace })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async marketplace(
    @common.Param() params: MarketplaceWhereUniqueInput
  ): Promise<Marketplace | null> {
    const result = await this.service.marketplace({
      where: params,
      select: {
        apiKey: true,
        createdAt: true,
        description: true,
        id: true,
        isActive: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Marketplace })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateMarketplace(
    @common.Param() params: MarketplaceWhereUniqueInput,
    @common.Body() data: MarketplaceUpdateInput
  ): Promise<Marketplace | null> {
    try {
      return await this.service.updateMarketplace({
        where: params,
        data: data,
        select: {
          apiKey: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Marketplace })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteMarketplace(
    @common.Param() params: MarketplaceWhereUniqueInput
  ): Promise<Marketplace | null> {
    try {
      return await this.service.deleteMarketplace({
        where: params,
        select: {
          apiKey: true,
          createdAt: true,
          description: true,
          id: true,
          isActive: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/marketplaceSyncs")
  @ApiNestedQuery(MarketplaceSyncFindManyArgs)
  async findMarketplaceSyncs(
    @common.Req() request: Request,
    @common.Param() params: MarketplaceWhereUniqueInput
  ): Promise<MarketplaceSync[]> {
    const query = plainToClass(MarketplaceSyncFindManyArgs, request.query);
    const results = await this.service.findMarketplaceSyncs(params.id, {
      ...query,
      select: {
        createdAt: true,
        externalId: true,
        id: true,

        marketplace: {
          select: {
            id: true,
          },
        },

        product: {
          select: {
            id: true,
          },
        },

        status: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/marketplaceSyncs")
  async connectMarketplaceSyncs(
    @common.Param() params: MarketplaceWhereUniqueInput,
    @common.Body() body: MarketplaceSyncWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      marketplaceSyncs: {
        connect: body,
      },
    };
    await this.service.updateMarketplace({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/marketplaceSyncs")
  async updateMarketplaceSyncs(
    @common.Param() params: MarketplaceWhereUniqueInput,
    @common.Body() body: MarketplaceSyncWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      marketplaceSyncs: {
        set: body,
      },
    };
    await this.service.updateMarketplace({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/marketplaceSyncs")
  async disconnectMarketplaceSyncs(
    @common.Param() params: MarketplaceWhereUniqueInput,
    @common.Body() body: MarketplaceSyncWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      marketplaceSyncs: {
        disconnect: body,
      },
    };
    await this.service.updateMarketplace({
      where: params,
      data,
      select: { id: true },
    });
  }
}
